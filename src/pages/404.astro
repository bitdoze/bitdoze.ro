---
import Layout from "../layouts/Layout.astro";
import { Icon } from "astro-icon/components";
---

<Layout title="Pagina nu a fost găsită" description="Pagina căutată nu există sau a fost mutată." noindex={true}>
  <section class="section-padding" style="min-height: 60vh; display: flex; align-items: center;">
    <div class="container text-center">
      <div class="mb-6">
        <Icon name="mdi:compass-off-outline" class="h-16 w-16 mx-auto" style="color: var(--color-primary);" />
      </div>
      <h1 class="text-4xl md:text-5xl font-bold mb-4">404 – Pagina nu a fost găsită</h1>
      <p class="text-lg mb-8" style="color: var(--color-text-light);">
        Ne pare rău, pagina pe care o cauți nu există sau a fost mutată.
      </p>

      <div class="flex flex-col sm:flex-row gap-3 justify-center">
        <a href="/" class="btn btn-primary btn-lg">
          Înapoi la Acasă
        </a>
        <a href="/contact" class="btn btn-secondary btn-lg">
          Contactează-ne
        </a>
      </div>
    </div>
  </section>

  <style>
    /* Respect reduced-motion on this page */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation: none !important;
        transition: none !important;
        scroll-behavior: auto !important;
      }
    }
  </style>
</Layout>

